node_modules/
.env
__pycache__/
*.py[cod]
.Python
.venv/
venv/
env/
*.egg-info/
.DS_Store
.vite/

# Production UI is committed so Render (native Python) can serve it
# without a successful Node build. Rebuild locally with: npm run build
