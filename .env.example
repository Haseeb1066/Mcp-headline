# Optional — polish the executive summary with an LLM
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Tableau Server / Cloud (required for workbook resolve + published datasource queries)
TABLEAU_SERVER=
# Site contentUrl (empty = default site)
TABLEAU_SITE_NAME=
TABLEAU_PAT_NAME=
TABLEAU_PAT_VALUE=

# Optional
# TABLEAU_REST_API_VERSION=3.27
# TABLEAU_SSL_VERIFY=0
# NODE_TLS_REJECT_UNAUTHORIZED=0

PORT=8787
